#F.U.P. que, dado um conjunto de N números, determine o menor valor, o maior valor e a soma dos valores. Faça com que ele aceite apenas números entre 0 e 1000.#
 
primo = int(input("digite o primeiro número: "))
ultimo = int(input("digite o último número: "))
soma = primo + ultimo
for c in range(primo, primo + 1, ultimo):
if ultimo > 1000:
print("número indisponível")
else:
print("o menor numero é:", primo)
print("o maor numero é:",ultimo)
print("a soma de ambos é:", soma)
